# Weighted (E1) at arity 2: the admissible cone for $h_2E$, the localized-layer law, and what the projection measures

*2026-08-17.  Extends [E1 admissible](E1_ADMISSIBLE.md) §6 (Part D of
`solve_e1.py`) to arity 2, per PLAN §6 open item "re-derive the
admissible leaves for the $h_2E$ zero set at arity 2 and project the
localized module".  All classification claims are verified in exact
rational arithmetic by `python3 solve_e1.py` (69 checks total; the new
Part F contributes 12) and `python3 audit_weighted_e1.py` (exact
isotropic block audit, 11 test measures + 3 negative controls).  Measurements and artifacts are listed
in §7–§8; escape-fingerprint context is
[Unprojected escape note](UNPROJECTED_ESCAPE_NOTE.md).*

## 0. Verdicts up front

1. **Classification (pure squares).**  A two-root leaf
   $\Phi=A(t_1,t_2,s)+\det(x_1,x_2,y)\,B(t_1,t_2,s)$ has
   $E_\mu[\text{CE-square}]=0$ for *every* isotropic $\mu$ iff
   $$A=\alpha(s)\,(t_1^2-\tfrac13)+\beta(s)\,(t_2^2-\tfrac13)
      +\gamma(s)\,(t_1t_2-\tfrac s3),\qquad
     B=\lambda(s)\,t_1+\nu(s)\,t_2 ,$$
   i.e. iff $\Phi=y^{\mathsf T}C(x_1,x_2)\,y$ for a root-equivariant
   traceless symmetric $C$.  Equivalently
   $E_\mu[\Phi\mid x_1,x_2]=\langle C(x_1,x_2),\mathrm{Dev}(\mu)\rangle$
   with $\mathrm{Dev}=\int yy^{\mathsf T}d\mu-\tfrac13I$: every
   admissible pure-square two-root block is a root-modulated pairing
   against the deviatoric tensor, and its expectation lies in the ideal
   generated by the five deviatoric coordinates.
2. **The mode-2 theta tower survives; mode 6 dies.**  By
   $T_n=sU_{n-1}-U_{n-2}$,
   $$\hat C_n=-2\,U_{n-2}(s)\,(t_1^2-\tfrac13)+2\,U_{n-1}(s)\,(t_1t_2-\tfrac s3),$$
   so every modulated mode-2 generator $\hat C_n$ — hence every theta
   atom $\Theta_q^{(2)}$ — is leaf-quadratic and stays
   weighted-admissible.  The mode-6 tower $\hat S_n$ (leaf degree 6) is
   cut from the pure layer entirely.
3. **The $h_2$-localized layer is *not* unconstrained.**  Value-level
   slackness is vacuous for $h_2\times$blocks ($h_2=0$ on the zero
   set), but **second-order** slackness at the $E$-zero family forces
   the *base value* of every $h_2$-multiplied block to vanish there:
   the h2loc layer must satisfy the **unweighted (E1)** conditions
   (§5).  At full sharpness the same expansion kills every Dev-linear
   pure square and the scalar $h_2$-multiplier — the sharp weighted
   certificate is carried almost entirely by the localized module.
4. **The projection does not, and cannot, kill the pole.**  Facial
   projection removes dual generators, i.e. *weakens* the moment-side
   constraints: every recession ray survives projection.  Measured at
   degree 14 (MOSEK dual, all-measures cone): baseline
   $-1.9198\times10^{-4}$; pure-layer weighted-(E1) projection →
   **dual unbounded** (recession ray found, collision-type `graph_4`
   content); h2loc-layer (E1) projection alone → bounded but
   $-4.8667\times10^{-3}$.  The escape growth itself is
   **$\ge95\%$ inside the admissible cone** at the smallest
   $\varepsilon$ interval (§7.3): the pole is genuine non-attainment
   *within* the sharp-face module.  The classification's operative use
   is therefore structural — it pins the sharp limit object and
   certifies which added objects (theta-atom window cuts, new weights)
   are sharpness-compatible — not a finite-degree bound accelerator.

## 1. Setup: the zero set of $W=h_2E$ and the affine-hull lemma

$W=h_2E$ with $h_2=\frac{3p_2-1}2=\frac32\operatorname{tr}\mathrm{Dev}^2\ge0$.
Zero set: $\{W=0\}=\{h_2=0\}\cup\{E=0\}$, and $\{h_2=0\}$ = all
isotropic antipodal measures; the known $E$-zero family (pole–equator)
is itself isotropic.  So the *unconditional* zero set available to
complementary slackness is the full isotropic slice — codimension 5,
vastly larger than the unweighted zero family.

**Lemma (affine hull).**  For every truncation degree, the moment
vectors of isotropic antipodal probability measures fill a relatively
open neighborhood of Haar's moment point inside the affine slice
$$\{m_0=1,\ m_2=0,\ m_\ell\ \text{free for even }\ell\ge4\}$$
of harmonic moment coordinates $m_\ell=(\int Y_{\ell k}\,d\mu)_k$.
*Proof.*  Densities $\mu_q=(1+q)\sigma$ with $q$ an even polynomial,
$\langle q,1\rangle=\langle q,Y_{2k}\rangle=0$, $\|q\|_\infty<1$: such
$\mu_q$ is an isotropic antipodal probability measure and
$m_\ell(\mu_q)=\langle Y_{\ell\cdot},q\rangle$ realizes every small
value of the free coordinates. $\square$

**Consequence.**  A label functional (any $E_\mu[\cdot]$ of the flag
algebra, polynomial in $m$) vanishes on all isotropic measures iff its
polynomial vanishes identically after substituting $m_0=1$, $m_2=0$ —
iff it lies in the ideal generated by the five deviatoric coordinates
(and $m_0-1$).  This is the exact form of the mission statement
"$e(\text{square})$ annihilates the affine hull of
$\{y(\mu):M_2(\mu)=I/3\}$", and it is what `solve_e1.py` Part F
computes: the `IsotropicSlice` engine parametrizes the slice by
$\varepsilon$-symbols per even harmonic basis element of degree
$\ge4$, evaluates any multi-sample moment polynomial exactly on it, and
demands the zero polynomial.

## 2. The arity-2 classification theorem

**Theorem.**  For a two-root flag square (any of the solver sectors
`two_root_*`, with or without the $(1-s^2)$ minor root weight), the
following are equivalent:

  (a) $E_\mu[\,(\text{conditional-expectation square})\,]=0$ for every
      isotropic antipodal probability measure $\mu$;
  (b) $E_\rho[\Phi(x_1,x_2;\cdot)]=0$ for every isotropic antipodal
      probability $\rho$ and **every** root pair
      $(x_1,x_2)\in S^2\times S^2$;
  (c) for every fixed root pair, $y\mapsto\Phi(x_1,x_2;y)$ is a pure
      degree-2 spherical harmonic;
  (d) $A=\alpha(s)(t_1^2-\tfrac13)+\beta(s)(t_2^2-\tfrac13)
      +\gamma(s)(t_1t_2-\tfrac s3)$ and $B=\lambda(s)t_1+\nu(s)t_2$
      with polynomial $\alpha,\dots,\nu$;
  (e) $\Phi=y^{\mathsf T}C(x_1,x_2)y$ on the spheres, with
      $$C=\alpha\,(x_1x_1^{\mathsf T}-\tfrac13I)
        +\beta\,(x_2x_2^{\mathsf T}-\tfrac13I)
        +\gamma\,(\mathrm{sym}(x_1x_2^{\mathsf T})-\tfrac s3 I)
        +\lambda\,\mathrm{sym}(x_1n^{\mathsf T})
        +\nu\,\mathrm{sym}(x_2n^{\mathsf T}),\quad n=x_1\times x_2 .$$

*Necessity* (a)$\Rightarrow$(b): squares are nonnegative, so (a) forces
the conditional expectation to vanish at $\mu$-a.e.\ root pair; running
$\mu=(1-t)\sigma+t\rho$ (isotropic for every isotropic $\rho$, full
support) decouples the roots from the measure: first $t=0$ gives the
Haar rows at all roots, then linearity in $t$ gives $E_\rho[\Phi]=0$
for all roots and all isotropic $\rho$.  (b)$\Rightarrow$(c) is the
affine-hull lemma applied in the leaf variable.  (c)$\Rightarrow$(d):
for fixed $s\in(-1,1)$ the pair $(t_1,t_2)$ ranges over an open planar
region, so the polynomial identity in $(t_1,t_2)$ holds
coefficient-wise, and in $s$ by density; the $\det$-even/odd split
matches the five quadratic harmonics available in the frame
$(x_1,x_2,n)$.  *Sufficiency* (e)$\Rightarrow$(a): free polynomial
identities (machine-checked modulo the three sphere ideals, with
$\operatorname{tr}C=0$ on the root spheres) give
$E_\rho[\Phi]=\operatorname{tr}(C\,M_2(\rho))
=\langle C,\mathrm{Dev}(\rho)\rangle=0$ at isotropic $\rho$, pointwise
in the roots — so the square's expectation vanishes.  The minor
sectors carry the extra root factor $1-s^2\ge0$, which vanishes only on
the measure-zero set $s=\pm1$; the same equivalences hold verbatim.

**Machine checks** (Part F of `solve_e1.py`; the "squeeze"):
predicted $\subseteq$ nullspace of exact necessary rows (harmonic tests
of degree $\ell\in\{0\}\cup\{4,6,8\}$ at rational root pairs with
enough distinct $s$ values), with equal dimensions, at working degree 8
on the full parity rings (21 of 85 even, 16 of 80 odd) and per solver
sector at degree 14.  The closed form also satisfies all unweighted
pole–equator rows (the weighted conditions refine (E1), as they must:
the pole–equator family is isotropic).

### Dimension table (solver degree 14: flag degree 7, minors 6)

| sector | weighted admissible / total | unweighted (E1) was |
|---|---:|---:|
| `two_root_even_00` | **9** / 30 | 19 |
| `two_root_even_11` | **9** / 30 | 20 |
| `two_root_odd_01` | **7** / 30 | 28 |
| `two_root_odd_10` | **7** / 30 | 28 |
| `two_root_even_00_minor` | **8** / 24 | 13 |
| `two_root_even_11_minor` | **7** / 20 | 13 |
| `two_root_odd_01_minor` | **6** / 20 | 19 |
| `two_root_odd_10_minor` | **6** / 20 | 19 |

Per sector the count is just the number of $s$-monomials of the right
parity that fit the degree cap: e.g. `even_00` holds
$\alpha,\beta\in\{s^0,s^2,s^4\}$, $\gamma\in\{s^1,s^3,s^5\}$.  Growth
with degree is *linear* (one new $s$-power per family per two degrees)
against quadratic sector growth: at solver degree 16 the majors are
11/45, 10/40, 8/40, 8/40 (minors 9/30, 9/30, 7/30, 7/30; spin-2 7/14);
at degree 18 the majors are 12/55, 12/55, 9/55, 9/55 (minors 11/45,
10/40, 8/40, 8/40; spin-2 8/19).  Files
`sdpa_runs/e1w_projection_deg{10,14,16,18}.json` (each re-verifies
nullspace = closed form before writing).

### Structural corollaries

* **$\hat C_n$ survives for every $n$** (verdict 2 above; exact
  identity checked for $n=0..8$).  The infinite-dimensional
  $\delta$-modulation freedom of the unweighted analysis persists in
  the weighted pure layer, but only on circle mode 2.  In particular
  the theta atoms $\Theta^{(2)}_q=\sum_nq^{n^2}\hat C_n$ are
  weighted-admissible term by term.
* **$\hat S_n$ (mode 6) and $T_6+\tfrac13$ die** in the pure layer
  (leaf degree 6): checked as negative controls, nonzero already on
  rotated-ONB mixtures.
* The Gram-determinant corrections are absorbed:
  $D-(1-s^2)/3\equiv-(t_1^2-\tfrac13)-(t_2^2-\tfrac13)
  +2s(t_1t_2-\tfrac s3)$ lies inside the span; no separate ideal
  family remains (unlike the unweighted case, the weighted conditions
  test all roots and all leaves, so $D$-multiples get no free pass).

## 3. Unrooted families (weighted)

Computed with the same isotropic-slice engine (exact
$\varepsilon$-polynomials over the affine hull; no sampling):

| family | weighted admissible | unweighted was |
|---|---|---|
| one-root spin 0/1/2/$\ge3$ | $\{t^2-\tfrac13\}$ / $\{t\}$ / $\{1\}$ / $\{0\}$ (Part D) | 2/2/2, spin 3 $\{t^3\}$ |
| pair flags | $\operatorname{span}\{3p_2-1\}$ | $\{3p_2-1,\ E\}$ |
| `harmonic_flag_2` | $\operatorname{span}\{1,\ t^2-\tfrac13\}$ | $\{1,T_2{+}\tfrac13,T_6{+}\tfrac13\}$ |
| `harmonic_flag_{l\ge4}` | $\operatorname{span}\{t^2-\tfrac13\}$ | $\{T_2{+}\tfrac13,T_6{+}\tfrac13\}$ |
| `spin2_flag` (cap 6) | dim **6** / 10 | 9 / 10 |

The weighted `spin2_flag` space is the **$M_2$-closure rule**: a flag
combination survives iff replacing every $M_2$-contraction substructure
by its isotropic value $\tfrac13$ cancels exactly.  Basis at cap 6:
$$\{(),\ (2),\ (2,2),\ ((1,1),1),\ (1,3;1)-\tfrac13(4),\
(2,4)-\tfrac13(4)\},$$
where e.g. $(2)$ survives because $\int(x\cdot y)^2d\mu(y)\equiv\tfrac13$
identically on the slice, and $(4)$ alone dies because 4th moments are
free.

## 4. What the pure layer of a sharp certificate can be

Combining §2–§3: every pure-square family of a sharp all-measures
$h_2E$ certificate is confined to contractions of the deviatoric
tensor with root-equivariant coefficients — the `--h2-localized-all`
structure of [H2 weighted experiment](H2_WEIGHTED_EXPERIMENT.md) is
forced at arity 2 as well, with the two-root pure layer reduced
$30\to9,\,30\to9,\,30\to7,\,30\to7$ (majors) and the spin-2/product
layers to the $M_2$-closed combinations.

## 5. Second-order slackness: the localized layer's law, and a stronger collapse

Value-level slackness says nothing about $h_2\times$blocks ($h_2=0$ on
the whole zero set).  But let $\mu^*$ be in the $E$-zero family and
$\mu_t=\mu^*+td+O(t^2)$ an admissible curve of probability measures.
Then $h_2(\mu_t)=\tfrac12\ddot h_2[d]\,t^2+O(t^3)$ (as $h_2\ge0$
vanishes quadratically, $\ddot h_2[d]=3\|\mathrm{Dev}(d)\|_F^2\ge0$) and
$E(\mu_t)=t\dot E[d]+O(t^2)$, so
$$W(\mu_t)=O(t^3)\quad\text{along every curve through the } E\text{-zero family}.$$
For a sharp certificate $W=\sum_bV_b$ (+identities) with every
$V_b\ge0$ valid for all measures, $V_b(\mu_t)\ge0$ and
$\sum_bV_b(\mu_t)=O(t^3)$ force the $t^0,t^1,t^2$ coefficients of each
$V_b$ to vanish.  The $t^2$ coefficients are:

* $h_2$-localized block $V=h_2\cdot B_{\text{base}}$:
  $\tfrac12\ddot h_2[d]\,B_{\text{base}}(\mu^*)$.  Choosing $d$ with
  $\ddot h_2[d]>0$:
  $$\boxed{\;B_{\text{base}}(\mu^*)=0\ \text{for every }\mu^*\text{ in
  the }E\text{-zero family}\;}$$
  — the **unweighted (E1) conditions** on the *base* of every
  localized block.  The h2loc two-root layer must live in the
  unweighted admissible bases (19/20/28/28 + minors 13/13/19/19), the
  h2loc spin-2 layer in its 9/10 space, the h2loc pair layer in
  $\{3p_2-1,E\}$.
* pure square $V=E[(\mathrm{CE})^2]$: $\int(\partial_d\mathrm{CE})^2$,
  so $\partial_d\mathrm{CE}(R)=0$ for $\mu^*$-a.e.\ roots and every
  direction.  With $\mathrm{CE}=\langle C(R),\mathrm{Dev}(\mu)\rangle$
  and $\mathrm{Dev}(d)$ spanning all traceless directions, this kills
  $C$ on $(\operatorname{supp}\mu^*)^2$ for every family member, hence
  $C\equiv0$: **at full sharpness every Dev-linear pure square dies**
  (two-root, one-root, and the Dev-linear spin-2 combinations).  The
  survivors are the Dev-quadratic squares — $(3p_2-1)$-type — whose CE
  vanishes to second order.
* scalar $h_2$ multiplier $\lambda\,g_2$:
  $\lambda\cdot\tfrac13\ddot h_2[d]$, so $\lambda=0$: even $h_2$
  itself cannot appear linearly; the $t^3$ budget
  $\tfrac12\ddot h_2[d]\,\dot E[d]$ is carried by the localized layer
  through its base *derivatives*.

So the sharp weighted certificate is, up to Dev-quadratic scalars,
**entirely the $h_2$-localized module over the unweighted-(E1) bases**.
The value-level weighted-(E1) classification (§2) remains the correct
necessary condition for whatever pure squares are kept at finite
degree, and the second-order law is implemented as an independent
projection of the localized layer (§6).  The full second-order system
away from the $E$-zero stratum (at isotropic $\mu^*$ with $E(\mu^*)>0$
there is positive budget $\tfrac12\ddot h_2E(\mu^*)$) is the (E2)
analogue left to the multiplier-layer program (PLAN §6 last item).

## 6. Implementation

* `solve_e1.py` **Part F** (69 checks total): exact harmonic bases
  (`harmonic_basis`), the `IsotropicSlice` engine, sampled-root
  necessity rows with distinct-$s$ sampling (`sample_root_pairs`),
  closed-form generators, free-identity sufficiency
  (`weighted_sufficiency_identities`), the $\hat C_n$ survival and
  $\hat S_1$ death checks, and the weighted spin2/harmonic-flag/pair
  computations.
* `solve_e1.py --export-projection PATH --solver-degree D --weighted`
  writes the weighted bases (two-root sectors, spin2, one-root, pair,
  harmonic-flag) after re-verifying nullspace = closed form at degree
  $D$; files `sdpa_runs/e1w_projection_deg{10,14,16,18}.json`.
* `sos_search.py --e1-project e1w_projection_degD.json` now detects
  the weighted file and projects the **pure** layer by the weighted
  bases (three-point radials, pair combos, harmonic-flag weights,
  spin2 and two-root vectors from the JSON).
* `sos_search.py --e1-project-localized e1_projection_degD.json`
  (new) projects the **h2loc copies** by the *unweighted* (E1) bases —
  the §5 law.  Without it the localized copies are built from the
  unprojected matrices (exempt at value level), including copies of
  families the pure projection dropped.
* `audit_weighted_e1.py`: exact rational audit that every projected
  pure basis vector has zero conditional expectation at the ONB, two
  continuous isotropic families (rotated-ONB mixtures; pole–equator
  quarter-turn families, isotropic for every angle/weight) and cross
  mixtures — at on- and off-support root pairs — plus
  $\mathrm{Dev}=0$, spin-2 combos $=0$, $3p_2-1=0$, $g_2=0$; negative
  controls $T_6+\tfrac13$, $\hat S_1$ nonzero, and a non-isotropic
  control violating 1237 conditions.  All pass; several test measures
  have $E>0$ (the audit is not confined to the $E$-zero family).
* `sdpa_runs/fingerprint_admissible_split.py`: splits selector
  certificate growth per h2loc block into (E1)-admissible vs dead
  components (§7.3).
* Regression tests: `test_sos_search.py` gains three weighted-(E1)
  tests (small-degree classification squeeze, sufficiency identities,
  weighted-export loader round trip); full suite 25 passed.

## 7. Measurements (degree 14, all-measures weighted cone, MOSEK dual, `--scale-constraints`)

Flags: `--h2-weighted-target --h2-localized-all --degree 14
--no-pointwise-sos --harmonics --three-point-flags --four-point-flags
--two-root-flags --rank-relations --dual`.

### 7.1 Bounds

| configuration | m | labels | bound for $h_2E$ |
|---|---:|---:|---:|
| baseline (no projection) | 806 | 1312 | $-1.9198\times10^{-4}$ |
| pure layer → weighted-(E1) | 456 | 1312 | **unbounded** (recession ray) |
| h2loc layer → unweighted-(E1) (`--e1-project-localized`) | 747 | 1291 | $-4.8667\times10^{-3}$ |
| both layers | 397 | 1270 | unbounded (a fortiori: projection weakens the moment side, so the pure-layer ray survives) |

The baseline reproduces RESULTS.md's all-measures row in the new $E$
normalization (the recorded $-3.816\times10^{-4}$ predates the
2026-08-17 rescale).  The pure-layer recession ray
(`sdpa_runs/ray_we1_deg14.json`; double precision,
`optimal_inaccurate`, squared norm $1.13\times10^4$; qualitative, like
the earlier ray extractions) is the weighted analogue of the unweighted
full-projection unboundedness of [E1 admissible](E1_ADMISSIBLE.md) §4 —
but a **different mode** than the famous $g_4$ escape: pairings
$\langle\text{target},\text{ray}\rangle=-1.000$ (normalization),
$g_4\cdot\text{ray}\approx0$, $g_6\cdot\text{ray}\approx0$, and
$g_2\cdot\text{ray}=+55.0$, with $L^1$ sector shares 55% $p_2\times$
products, 20% `graph_4`, 15% pair, 8% triangle.  The escape *carries*
$h_2>0$ and runs through the product labels: at finite degree the
pure-layer "dead" squares were doing real work pinning the base labels
that the $h_2$-localized blocks couple to products; cutting them frees
an $h_2$-positive product-sector direction.  Solve summaries:
`sdpa_runs/mosek_deg14_h2w_h2all_am_{baseline,le1}.json`.

**Reading.**  A facial projection removes dual generators; on the
moment side every feasibility constraint weakens, so bounds can only
degrade and rays can only appear.  Moreover the ray has zero constant
coordinate and target pairing $-1$, so the projected dual is unbounded
at **every** $\varepsilon$: at degree 14 *no* $\varepsilon$-certificate
of any accuracy exists inside the sharp-face pure cone — the dead
directions are load-bearing at finite degree, at every accuracy.  "The
sharp certificate lives in the admissible cone" is a statement about
the $\varepsilon\to0$ *limit* object; the projected cones are the right
home for the *cut-augmented* programs (theta-atom window cuts, gap
cuts) that must restore $\varepsilon$-feasibility, not for plain bound
runs.

### 7.2 Exact audits

`audit_weighted_e1.py`: every projected pure block vanishes exactly
(rational zero) at all 11 isotropic test measures, including measures
with $E>0$ (e.g. rotated-ONB mixture $t=\tfrac13,u=\tfrac14$:
$E=36864/714025$); the h2loc layer vanishes through its scalar $h_2$
factor; negative controls nonzero; a non-isotropic measure violates
1237 conditions.

### 7.3 The escape is admissible: the split verdict

`fingerprint_admissible_split.py` on the selector certificates of the
KKT-inclusive weighted problem (`sel_h2all_{1em3,1em4,5em5}.result`),
splitting the growth $\Delta Y$ of each h2loc block into unweighted-(E1)
admissible and dead components
(`sdpa_runs/fingerprint_admissible_split_e1.json`):

| interval | $\|\Delta Y\|_F$ | admissible | dead | dead share |
|---|---:|---:|---:|---:|
| $10^{-3}\to10^{-4}$ | 1864.4 | 1829.2 | 360.8 | 19.4% |
| $10^{-4}\to5.3\times10^{-5}$ | 3974.7 | 3971.0 | 170.5 | **4.3%** |

The static certificate at $10^{-3}$ still has a 20.9% dead share; the
*growth* is increasingly confined to the admissible cone as
$\varepsilon$ shrinks.  Together with §7.1:

> the $\varepsilon$-certificates converge into the sharp face and the
> pole is carried by **admissible** content — the h2-localized
> modulated ($\hat C_n$/$\hat S_n$) tower itself.  No facial reduction
> can remove it; the missing objects are *additional valid
> constraints* on that tower (the $h_2$-localized theta-atom window
> cuts of [Unprojected escape note](UNPROJECTED_ESCAPE_NOTE.md) item
> (a), whose admissibility both layers' classifications here certify)
> or a further weight.

This answers the fingerprint question directly: the escape direction
$D$ is *not* blocked by the projection — structurally it cannot be,
and empirically its mass is 81→96% inside the admissible span.

## 8. Exports (GMP-ready, exact rational; shift $+2/3$)

| file | m | labels | note |
|---|---:|---:|---|
| `deg14_h2w_h2all_am.dat-s` | 806 | 1312 | unprojected all-measures baseline (pre-existing) |
| `deg14_h2w_h2all_am_we1.dat-s` | **397** | 1270 | both layers projected; $\varepsilon=0$ dual unbounded (§7.1) — for cut-augmented runs |
| `deg14_h2w_h2all_am_le1.dat-s` | **747** | 1291 | h2loc layer projected only; bounded ($-4.87\times10^{-3}$ double) |
| `deg14_h2w_h2all_am_pe1.dat-s` | **456** | 1312 | pure layer only (the literal B3 config; dual unbounded at every $\varepsilon$) |
| `deg16_h2w_h2all_am_we1.dat-s` | **607** | 2029 | both layers projected (KKT-inclusive unprojected deg-16 reference: m = 1243) |
| `deg16_h2w_h2all_am_le1.dat-s` | **1173** | 2040 | h2loc layer only — the **bounded** degree-16 sharp-face bound target |
| `deg18_h2w_h2all_am_we1.dat-s` | **1015** | 3484 | both layers projected — **degree 18 is m-reachable** (below the m ≈ 1250 that degree 16 costs unprojected) |

(m = "variables" in the export log; the reported bound =
objValPrimal + objective_shift, shift $2/3$.)  The two-layer
projection shrinks degree 14 from m = 806 to **397** (pure blocks
$30\to9/9/7/7$ etc., h2loc blocks $30\to19/20/28/28$) and degree 16 to
**607** — *below* the unprojected degree-14 size, i.e. two full degree
steps come for free inside the sharp-face cone.  The h2loc-only deg-16
variant is reproduced by the same command with only
`--e1-project-localized e1_projection_deg16.json`.

## 9. What to do with this

1. **Stack cuts on the projected cone.**  The theta-atom window cuts
   ($h_2$-localized diagonal atoms $\sum_nq^{n^2}$ with rational tail
   majorants) are valid and — by §2–§5 — sharpness-compatible in both
   layers.  They are exactly the objects the admissible escape of §7.3
   calls for; the projected exports of §8 are their intended home.
2. **Degree-16/18 weighted runs** shrink accordingly under the
   two-layer projection (the admissible sector dimensions grow linearly
   in the degree against quadratic for the full sectors); measured m
   values are in §8 as the exports complete.
3. **(E2) program.**  §5's second-order system at general isotropic
   points (positive budget $\propto\ddot h_2\,E$) is the natural next
   derivation target: it constrains the localized layer's base
   *derivatives* and should determine the profile coefficients
   $\gamma_n$ of the escaping tower.
